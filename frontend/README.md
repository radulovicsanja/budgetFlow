# BudgetFlow Frontend

React + Vite klijent za Spring Boot backend.

## Pokretanje

1. Backend na `http://localhost:8083`
2. Frontend:

```bash
cd frontend
npm install
npm run dev
```

Otvori `http://localhost:5173`

Vite proxy proslijedi `/api` i `/auth` na backend.
